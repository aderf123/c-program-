// Task 3: Write a C program to calculate the area of a rectangle
#include <stdio.h>

int main() {
    int length = 10;
    int width = 4;
    int area;

    area = length * width;

    printf("Area = %d\n", area);

    return 0;
}